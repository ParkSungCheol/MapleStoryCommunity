var element = document.getElementById("golem");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/골렘.png', 'popup', spec);
                });
                
var element = document.getElementById("forest");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/북쪽숲.png', 'popup', spec);
                });

var element = document.getElementById("gold");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/골드비치.png', 'popup', spec);
                });

                var element = document.getElementById("under");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/커닝시티.png', 'popup', spec);
                });
                var element = document.getElementById("fired");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/불타버린땅.png', 'popup', spec);
                });

var element = document.getElementById("treasure");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/유적발굴지.png', 'popup', spec);
                });

                var element = document.getElementById("quiet");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/빅토리아아일랜드/조용한습지.png', 'popup', spec);
                });

                var element = document.getElementById("eos1~30");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/에오스탑1층~30층.png', 'popup', spec);
                });
                
                var element = document.getElementById("eos31~60");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/에오스탑31층~60층.png', 'popup', spec);
                });
                
                var element = document.getElementById("helios2~30");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/헬리오스2~30.png', 'popup', spec);
                });
                
                var element = document.getElementById("eos61~100");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/에오스61~100.png', 'popup', spec);
                });
                
                var element = document.getElementById("helios31~70");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/헬리오스31~70.png', 'popup', spec);
                });
                
                var element = document.getElementById("helios71~100");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/헬리오스71~100.png', 'popup', spec);
                });
                
                var element = document.getElementById("toy");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/루디브리엄/장난감공장.png', 'popup', spec);
                });
                
                var element = document.getElementById("sky");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/하늘숲입구.png', 'popup', spec);
                });
                
                var element = document.getElementById("bear");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/야생곰의영토.png', 'popup', spec);
                });
                
                var element = document.getElementById("rednose");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/빨간코해적단.png', 'popup', spec);
                });

                var element = document.getElementById("plant");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/50년된약초값.png', 'popup', spec);
                });
                
                var element = document.getElementById("kind");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/묘선.png', 'popup', spec);
                });
                
                var element = document.getElementById("monkey");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/원공.png', 'popup', spec);
                });
                
                var element = document.getElementById("palace");
                element.addEventListener('click', () => {
                    let width = 500;
                    let height = 445;
                    let top = (window.innerHeight - height) / 2 + screenY;
                    let left = (window.innerWidth - width) / 2 + screenX;
        
                    let spec = 'status=no, menubar=no, toolbar=no, resizable=no';
                    spec += ', width=' + width + ', height=' + height;
                    spec += ', top=' + top + ', left=' + left;
        
                    window.open('메이플몬스터/무릉/황금사원.png', 'popup', spec);
                });
                
               